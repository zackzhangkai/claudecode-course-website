name = "claudecode-7days"
type = "static"

[build]
  command = "npm run build"
  cwd = "."
  watch_dir = "./"

[deploy]
  directory = "./out"

# Required by Pages
pages_build_output_dir = "./out"

# Node.js version for Cloudflare Pages build environment
[build.environment]
  NODE_VERSION = "18"
